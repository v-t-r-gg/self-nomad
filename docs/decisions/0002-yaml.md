# ADR 0002: Use PyYAML safe loading

Status: accepted for v0.1

Use `yaml.safe_load` and strict Pydantic models. Comment-preserving rewrites are
not yet needed because portable documents are generated once and subsequently
read without implicit rewriting. Revisit if schema migrations must preserve
comments.

